/* Sunset · Canvas App
   Renders all email components in artboards using the shared DesignCanvas.
   
   Pattern (see welcome-flow/app.jsx for a real example):
     const App = () => {{
       return (
         <DesignCanvas title="..." subtitle="...">
           <DCSection id="..." title="...">
             <DCArtboard id="e1" label="..." width={{MW}} height={{...}}>
               <window.Email1 />
             </DCArtboard>
             ...
           </DCSection>
         </DesignCanvas>
       );
     }};
     ReactDOM.createRoot(document.getElementById("root")).render(<App />);
*/

// const App = () => { ... };
// ReactDOM.createRoot(document.getElementById("root")).render(<App />);
